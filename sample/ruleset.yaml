rules:
  my-rule:
    description: "Schemas references should not end with `.json`."
    given: "$.components.schemas.*.'$ref'"
    resolved: true
    severity: error
    then:
      function: pattern
      functionOptions:
        notMatch: .+\.json$
