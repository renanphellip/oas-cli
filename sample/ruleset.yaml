rules:
  my-first-rule:
    description: "abc"
    message:
      - abc
      - def
    given: "$.paths.*.*.responses"
    severity: error
    then:
      function: "testing"
      functionOptions:
        myOption: "testing"
  my-second-rule:
    description: "def"
    message: "{{error}}"
    documentation:
      - xpto
      - xyz
    given: "$.paths.*.*.responses"
    then:
      function: "testing"
      functionOptions:
        myOption: "testing"
  my-third-rule:
    description: "def"
    message: "{{error}}"
    given: $.paths./store/orders.get.parameters[0].name
    then:
      function: "casing"
      functionOptions:
        type: "camel"
        disallowDigits: true